import 'package:flutter/material.dart';

class AppColors {
  // Primary brand
  static const Color medication = Color(0xFF6C63FF);

  // Backgrounds
  static const Color background = Color(0xFFFAFAF8);
  static const Color surface = Color(0xFFFAFAF8);
  static const Color surfaceCard = Color(0xFFFFFFFF);

  // Semantic
  static const Color positiveEffect = Color(0xFF00B894);
  static const Color negativeEffect = Color(0xFFE17055);

  // Light tints
  static const Color positiveEffectLight = Color(0xFFE6F8F4);
  static const Color negativeEffectLight = Color(0xFFFDF0ED);

  // Text
  static const Color textPrimary = Color(0xFF1A1A2E);
  static const Color textSecondary = Color(0xFF6B6B80);

  // Borders
  static const Color border = Color(0xFFE8E8EE);
}
